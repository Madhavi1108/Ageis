# Base URL of the AEGIS backend API. Can also be overridden at runtime in the
# dashboard's System Settings screen (persisted to localStorage).
VITE_API_BASE_URL=http://localhost:8000

# NOTE: the optional dev API key is entered in System Settings, never here and
# never committed. Auth is disabled by default on the backend.
